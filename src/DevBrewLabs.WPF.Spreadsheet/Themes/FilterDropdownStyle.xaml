<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DevBrewLabs.WPF.Spreadsheet.Filtering">

    <Style x:Key="FilterMenuItemButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Padding" Value="24,4,12,4" />
        <Setter Property="Margin" Value="0,1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#e6f0fa" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#cce0f5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:FilterDropdown}">
        <Setter Property="Background" Value="White" />
        <Setter Property="BorderBrush" Value="#cccccc" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="#333333" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Width" Value="240" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:FilterDropdown}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2"
                            SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="6" Opacity="0.15" ShadowDepth="3" Direction="270" />
                        </Border.Effect>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Button x:Name="PART_SortAscending" Grid.Row="0" Style="{StaticResource FilterMenuItemButton}" Margin="0,4,0,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE174;" VerticalAlignment="Center" Margin="-16,0,8,0" Width="16" TextAlignment="Center" Foreground="#777" />
                                    <TextBlock Text="Sort A to Z" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                            
                            <Button x:Name="PART_SortDescending" Grid.Row="1" Style="{StaticResource FilterMenuItemButton}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE174;" VerticalAlignment="Center" Margin="-16,0,8,0" Width="16" TextAlignment="Center" Foreground="#777" RenderTransformOrigin="0.5,0.5">
                                        <TextBlock.RenderTransform>
                                            <ScaleTransform ScaleY="-1"/>
                                        </TextBlock.RenderTransform>
                                    </TextBlock>
                                    <TextBlock Text="Sort Z to A" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                            
                            <Rectangle Grid.Row="2" Height="1" Fill="#e0e0e0" Margin="4,4" />

                            <Grid Grid.Row="3" Margin="8,4,8,6">
                                <TextBox x:Name="PART_SearchBox" Padding="20,3,4,3" BorderBrush="#cccccc" BorderThickness="1" />
                                <TextBlock Text="&#xE11A;" FontFamily="Segoe MDL2 Assets" Foreground="#999999" Margin="6,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" IsHitTestVisible="False" FontSize="11" />
                            </Grid>

                            <CheckBox x:Name="PART_SelectAll" Grid.Row="4" Margin="12,0,8,4" Content="(Select All)" />

                            <ListBox x:Name="PART_ValuesList" Grid.Row="5" Margin="8,0,8,8" Height="200" BorderBrush="#cccccc" BorderThickness="1" ScrollViewer.HorizontalScrollBarVisibility="Hidden">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Padding" Value="4,3" />
                                        <Setter Property="Background" Value="Transparent" />
                                        <Setter Property="BorderThickness" Value="0" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter />
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#e6f0fa" />
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Content="{Binding DisplayText}" IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center" />
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <Rectangle Grid.Row="6" Height="1" Fill="#e0e0e0" Margin="4,0" />

                            <Grid Grid.Row="7" Margin="8,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Button x:Name="PART_ApplyButton" Grid.Column="1" Content="OK" Width="70" Margin="0,0,8,0" Padding="4,4" Background="#e1e1e1" BorderThickness="1" BorderBrush="#adadad" />
                                <Button x:Name="PART_CancelButton" Grid.Column="2" Content="Cancel" Width="70" Padding="4,4" Background="#e1e1e1" BorderThickness="1" BorderBrush="#adadad" />
                            </Grid>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
